# API
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Auth (feature toggle)
# If AUTH_REQUIRED=true, protected routes will require a valid JWT
AUTH_REQUIRED=false
JWT_SECRET="change-me-dev-secret"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Database
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?schema=public
DATABASE_URL="postgresql://sju:sju@localhost:5432/sju_dev?schema=public"

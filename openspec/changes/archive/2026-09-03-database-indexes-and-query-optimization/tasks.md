## 1. Actualización de Esquema Prisma

- [x] 1.1 Agregar índices compuestos en `EventParticipant`, `EventAnnotation` y `User` en `apps/api/prisma/schema.prisma`.
- [x] 1.2 Ejecutar `npx prisma validate` para certificar la integridad del esquema.

## 2. Certificación de Pruebas y Compilación

- [x] 2.1 Verificar que la suite de pruebas unitarias (`npm test`) apruebe al 100%.
- [x] 2.2 Certificar la compilación de producción con `npm run lint`.

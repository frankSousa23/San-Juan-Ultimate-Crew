# Entorno de ejecución (production | development)
NODE_ENV=development

# Puerto del servidor
PORT=3000

# URL de conexión a la base de datos PostgreSQL (opcional, en desarrollo usa almacenamiento integrado de demostración si no está definida)
DATABASE_URL=

# Clave secreta para firmar tokens JWT
JWT_SECRET=tu_clave_secreta_super_segura_de_al_menos_32_caracteres_12345

# URL base del frontend para redirecciones y CORS
FRONTEND_URL=http://localhost:3000
LOG_LEVEL=info

